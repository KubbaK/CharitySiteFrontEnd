const token = jwtcookie.jwt
    const decoded = jwtDecode(token)
    const id = decoded
    console.log(jwtcookie.jwt)
    console.log(decoded)
    console.log(id)


    const [data, setData] = useState([]);
    useEffect(() => {
        axios.get("http://localhost:5012/v1/Account/isLogged",{headers:{'Bearer': jwtcookie.jwt}})
        .then(response => {
         setData(response.data)}
        );
        console.log(data)
      },[]);